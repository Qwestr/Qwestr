import Firebase from './index'

describe('Firebase', () => {
  let firebase = true

  beforeEach(() => {
    // firebase = new Firebase()
  })

  it('should exist!', () => {
    expect(firebase).toBeTruthy()
  })
})
